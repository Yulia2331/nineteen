'use client'
import React, { useCallback, useEffect, useState, useRef } from 'react'
export default function Test({props}) {
    return (
        <div className="">
            <h1 className="text-7xl">Test {props.title}</h1>
        </div>
    )
}